## 1. 修改 export_version_sql

- [x] 1.1 在 `export_version_sql` 函数末尾添加编程对象导出逻辑（已存在）
- [x] 1.2 检查 `snapshot.routines` 是否为空，为空时跳过（已存在）
- [x] 1.3 复用 `export_routines_sql` 的 SQL 格式生成编程对象 SQL（已存在）

## 2. 修改 export_upgrade_sql

- [x] 2.1 按数据库类型过滤出兼容的编程对象
- [x] 2.2 构建旧版本和新版本的编程对象 Map（name + type 为 key）
- [x] 2.3 检测新增：遍历新版本，找出旧版本中不存在的
- [x] 2.4 检测删除：遍历旧版本，找出 新版本中不存在的
- [x] 2.5 检测修改：遍历新版本，找出旧版本中存在但 body 不同的
- [x] 2.6 导出新增编程对象的 CREATE 语句
- [x] 2.7 导出删除编程对象的 DROP 语句
- [x] 2.8 导出修改编程对象的 DROP + CREATE 语句

## 3. 验证

- [ ] 3.1 测试导出版本 SQL 包含编程对象
- [ ] 3.2 测试升级脚本包含新增编程对象
- [ ] 3.3 测试升级脚本包含删除编程对象
- [ ] 3.4 测试升级脚本包含修改编程对象
