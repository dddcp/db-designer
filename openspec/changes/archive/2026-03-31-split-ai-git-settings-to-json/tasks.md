## 1. 后端本地设置存储

- [x] 1.1 在 Rust 后端新增 `settings.json` 的读取、写入和删除能力
- [x] 1.2 新增仅面向 AI/Git 本地配置的 Tauri 命令，并注册到 `lib.rs`
- [x] 1.3 保持现有 SQLite 设置命令继续服务于非 AI/Git 设置

## 2. 前端接入本地设置

- [x] 2.1 将 AI 设置页改为读取和保存本地 JSON 配置
- [x] 2.2 将 Git 设置页改为读取、保存和清空本地 JSON 配置
- [x] 2.3 检查其他 AI/Git 配置使用点，改为读取本地 JSON 配置

## 3. 兼容性与验证

- [x] 3.1 处理 `settings.json` 不存在时返回空配置并在首次保存时创建文件
- [x] 3.2 决定并实现旧 SQLite `ai_*` / `git_*` 数据的迁移或忽略策略
- [x] 3.3 验证分享 `db_designer.db` 时不再包含 AI/Git 本地配置
